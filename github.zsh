#### GitHub CLI aliases

# Create pull request
alias pull="gh pr create --fill"
# View pull request
alias brpr="gh pr view --web"
# Merge pull request
alias merge="gh pr merge --delete-branch --merge && cleanup"
# Browse repository
alias browse="gh browse"
